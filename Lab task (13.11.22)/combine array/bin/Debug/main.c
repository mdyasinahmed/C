#include <stdio.h>
#include <stdlib.h>
#define max_size 100

int main()
{
    int a[max_size], b[max_size], c[max_size];
    int i, n, m =(n+n);
    scanf("%d", &n);

    for(i=0;i<n;i++){
        scanf("%d", &a[i]);
    }
    for(i=0;i<n;i++){
        scanf("%d", &b[i]);
    }


    return 0;
}
