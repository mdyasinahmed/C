// Comments in C
#include <stdio.h>

int main()
{
    // create a variable
    int data = 34;

    // print the value of data variable
    printf("Data = %d\n", data);

    // Prevent executing code using Comments

    int age;
    double height;

    printf("Enter the age: ");
    scanf("%d", &age);

    // printf("Enter the height: ");
    // scanf("%lf", &height);

    printf("Age= %d\n", age);
    //printf("Height= %lf\n", height);

    // Multiline Comments in C Programming
    /* this program takes age input from the user
    it takes user age  */

    // Why comment is important ??
    return 0;
}

// Comments are hints that makes our code or program easier to understand
// the line starts with two forward slashes are comments
