#include <stdio.h>
#include <stdlib.h>

int main()
{
    double a, b, d;
    scanf("%lf%lf", &a, &b);
    printf("%lf\n", (d = ceil(a));
    printf("%.4lf\n", b=floor());
    printf("%.4lf\n", abs(b));

    return 0;
}
